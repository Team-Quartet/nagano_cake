<div class="panel" style="margin-top: 50px;">
      <div class="panel-heading">
        <h5 class="panel-title">ジャンル検索</h5>
      </div>
        <% genres.each do |genre| %>
          <div class="panel-body">
            <%= link_to genre.name, items_path(genre_id: genre.id) %>
          </div>
        <%end%>
    </div>